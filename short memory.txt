Balance the classes (undersample/oversample) → see immediate accuracy lift

Add notch filter & simple artifact rejection → cleaner EEG → better features

Tune SSA parameters (window, n_imfs) or re-implement Ci-SSA → sharper components

Fix entropy parameters (m, r, fixed bins) → consistent complexity measures

Seed & stratify CV + BHHO → reproducible, stable scores

Try SVM as the fitness classifier → see if margin-based model helps

Tune KNN k → small gain